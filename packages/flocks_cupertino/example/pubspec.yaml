name: flocks_cupertino_example
description: "Exemplo do flocks_cupertino — os mesmos AppIconToken, servidos pelos glifos do cupertino_icons."
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flocks: ^0.1.0
  flocks_cupertino: ^0.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
