/// Ícones do Material Design como `AppIconProvider` do Flocks.
///
/// Existe **fora** do `flocks` de propósito: o core não importa
/// `material.dart`, e um teste de arquitetura barra quem tentar. É essa regra
/// que sustenta a tese de zero Material — e é este pacote que prova que ela não
/// custa flexibilidade nenhuma.
///
/// Serve também de implementação de referência: um `flocks_fontawesome` ou o
/// provider do set da sua empresa têm esta mesma forma.
library;

export 'src/material_icon_provider.dart';
