name: flocks_example
description: "Exemplo do flocks — uma semente de marca e os eixos globais restilizando os componentes."
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flocks: ^0.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
