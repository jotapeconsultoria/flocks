/// Lado do FAB isolado ao lado da barra flutuante do `AppNavigationFooter`
/// (respeita RTL via start/end).
enum AppNavFooterFabAlignment {
  /// FAB antes da barra (à esquerda em LTR).
  start,

  /// FAB depois da barra (à direita em LTR).
  end,
}
