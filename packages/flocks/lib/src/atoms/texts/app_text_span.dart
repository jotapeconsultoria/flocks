import 'package:flutter/widgets.dart';

/// The class that defines the AppTextSpan element.
final class AppTextSpan extends TextSpan {
  const AppTextSpan({super.children, super.style, super.text});
}
