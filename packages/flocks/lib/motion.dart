/// Camada de motion do Flocks: reduce-motion ([AppMotion]) e widgets/mixins de
/// micro-interação. Tokens de duração/curva ([AppDurations]/[AppCurves]) vivem
/// em `tokens.dart`. Só sobre a camada `widgets` (sem Material/Cupertino).
library;

export 'src/motion/motion.dart';
